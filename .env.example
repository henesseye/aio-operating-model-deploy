# ===========================================
# AIO Betriebsmodell - Environment Konfiguration
# ===========================================
# Kopiere diese Datei nach .env und passe die Werte an:
#   cp .env.example .env
#
# WICHTIG: .env enthält sensible Daten und wird NICHT ins Git committed!

# ===========================================
# Docker Registry & Version
# ===========================================

# Docker Registry (mit trailing slash!)
# Für Docker Hub: henesseye/
# Für Kanton SO: harbor-aio.so.ch/aio/
DOCKER_REGISTRY=harbor-aio.so.ch/aio/

# Image Version (KEINE latest Tags in Produktion!)
VERSION=1.0.6

# ===========================================
# Zentraler nginx-proxy (Produktion)
# ===========================================

# Virtual Host - wird vom zentralen nginx-proxy verwendet
# um eingehende Requests an diese Applikation zu routen
VHOST=aio-betriebsmodell.so.ch

# Virtual Port - interner Port der Applikation (Standard: 8080)
VPORT=8080

# ===========================================
# Sicherheit (MUSS GEÄNDERT WERDEN!)
# ===========================================

# JWT Secret für Token-Signierung (min. 32 Zeichen!)
# WICHTIG: In Produktion einen sicheren, zufälligen Wert verwenden!
JWT_SECRET=CHANGE_ME_min_32_characters_long

# Admin Passwort (wird beim ersten Start gesetzt)
# Username ist immer: admin
# WICHTIG: In Produktion ein sicheres Passwort verwenden!
ADMIN_PASSWORD=CHANGE_ME_secure_password

# ===========================================
# Darstellung (Optional)
# ===========================================

# Headless-Modus für Embedding in andere Anwendungen
# true = Ohne Header (Logo, Titel) und Admin-Button
# false = Normale Ansicht (Standard)
# Kann auch per URL-Parameter überschrieben werden: ?headless=true
VITE_HEADLESS_MODE=false

# ===========================================
# Nur für lokale Entwicklung (docker-compose.yml)
# ===========================================

# Externer Port für lokales Testing (nicht für Produktion)
# PUBLIC_PORT=8080
