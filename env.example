# AIO Operating Model - Production Environment Configuration
# Kopieren Sie diese Datei zu .env und passen Sie die Werte an

# ===== SICHERHEIT (MUSS GEÄNDERT WERDEN!) =====

# JWT Secret für Authentifizierung
# Mindestens 32 Zeichen, sicher und zufällig
JWT_SECRET=CHANGE-THIS-TO-A-SECURE-JWT-SECRET-AT-LEAST-32-CHARACTERS-LONG

# Admin-Passwort für den Login
# Username ist immer: admin
ADMIN_PASSWORD=CHANGE-THIS-TO-A-SECURE-ADMIN-PASSWORD

# ===== DOCKER REGISTRY KONFIGURATION =====

# Image Version (ERFORDERLICH - keine latest Tags erlaubt!)
# Aktuelle Version siehe: https://hub.docker.com/r/henesseye/aio-operating-model-api/tags
VERSION=1.0.4

# Internes Docker Registry (für Büro-Deployment)
# Mit trailing slash! z.B.: registry.example.com:5000/
# Leer lassen für direkten Pull von Docker Hub (henesseye/)
DOCKER_REGISTRY=

# ===== NETZWERK =====

# Öffentlicher Port der Web-Anwendung
# Standard: 80, bei Konflikten ändern (z.B. 8080, 443)
PUBLIC_PORT=80

# ===== BEISPIEL-KONFIGURATION =====

# Für direkten Docker Hub Pull:
# VERSION=1.0.4
# DOCKER_REGISTRY=
# JWT_SECRET=MySecureJWTKey2024ForAIOOperatingModelProduction!
# ADMIN_PASSWORD=SecureAdminPassword2024!
# PUBLIC_PORT=80

# Für internes Registry:
# VERSION=1.0.4
# DOCKER_REGISTRY=docker-registry.internal.company.com:5000/aio/
# JWT_SECRET=MySecureJWTKey2024ForAIOOperatingModelProduction!
# ADMIN_PASSWORD=SecureAdminPassword2024!
# PUBLIC_PORT=80